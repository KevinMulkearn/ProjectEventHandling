<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mulkearn.kevin.randomColourNames">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.mulkearn.kevin.randomColourNames.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.mulkearn.kevin.randomColourNames.AboutActivity"
            android:theme="@style/AboutStyle">
        </activity>
        <activity android:name="com.mulkearn.kevin.randomColourNames.RecentColorsActivity"
            android:theme="@style/RecentColorStyle">
        </activity>
    </application>

</manifest>